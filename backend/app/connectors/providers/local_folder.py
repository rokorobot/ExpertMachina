# LocalFolderProvider: filesystem discovery + file reading only.
# Populated by Steps 3-8 of the retrofit as logic is extracted out of
# framework.py. Until then, the framework still carries the filesystem
# concerns mapped in the Phase 1 concern map.
