# The human-readable dependency manifest (audit T2.2, dependency custody -
# docs/audit-2026-07-07.md, finding H-DEP-1).
#
#   THE CUSTODY RULE: dependency drift must be explicit and reviewable.
#   Edit THIS file (direct pins only), then regenerate the lock:
#
#     uv pip compile requirements.txt --universal --generate-hashes \
#         --python-version 3.11 -o requirements.lock
#
#   CI installs from requirements.lock with --require-hashes - the full
#   transitive tree, hash-verified. A requirements.txt edit without a
#   matching lock regeneration fails CI at install. pip-audit gates every
#   push; accepted findings are documented at the CI step, each with a
#   reason and a review-by date - never silent.

fastapi==0.136.3
uvicorn==0.49.0
sqlalchemy==2.0.30
pydantic==2.13.4
# 0.0.9 -> 0.0.31 (T2.2 security bump): 7 CVEs incl. CVE-2024-53981; this
# library parses multipart uploads at the FastAPI boundary - attacker input.
python-multipart==0.0.31
qdrant-client==1.9.1

# Document parsing fallbacks (imported by ingestion.py and
# connectors/framework.py _extract_text - previously undeclared)
python-docx==1.2.0
# 6.13.2 -> 6.13.3 (T2.2 security bump): GHSA-jm82-fx9c-mx94, patch release.
pypdf==6.13.3
llama-index-core==0.10.44
llama-index-vector-stores-qdrant==0.2.0
llama-index-embeddings-openai==0.1.9
llama-index-llms-openai==0.1.22

# NLI answer verification (Phase 3 verifier - optional; engine falls back to keyword overlap without these)
torch==2.12.0
transformers==5.11.0
sentencepiece==0.2.1

# MCP Agent Gateway (MVP 0.9 - read-only transport over Governance Contract v1)
mcp==1.27.2

# Provider adapters behind the D19 resolver (v1.1 WS1, package_consumer arc).
# openai was previously transitive via llama-index-llms-openai; pinned
# explicitly now that llm.py imports it directly. Credentials stay
# environment-based (OPENAI_API_KEY / ANTHROPIC_API_KEY) per D19.
openai==2.41.0
anthropic==0.109.1

# Credential custody layer (v1.2.0 WS0, D25): envelope encryption of
# outbound secrets under EM_SECRET_KEY. Previously transitive; pinned
# explicitly now that app/custody.py imports Fernet directly.
cryptography==48.0.1

# Suite harness (audit 2026-07-07, T0.1/T0.2): pytest discovers and runs
# every backend/test_*.py as an isolated subprocess - CI no longer
# enumerates suites by hand (the mechanism that orphaned 18 of 64).
# 8.4.1 -> 9.0.3 (T2.2 security bump): CVE-2025-71176; dev tool, harness
# usage is minimal (parametrize/fail) - verified by the full sweep.
pytest==9.0.3
